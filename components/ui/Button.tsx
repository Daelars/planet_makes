import React from 'react';

type ButtonProps = {
  // ...existing code...
};

const Button: React.FC<ButtonProps> = (props) => {
  return (
    <button>
      {/* ...existing code... */}
    </button>
  );
};

export default Button;
